const CLAIM_STATUS = Object.freeze({
    DRAFT: "DRAFT",
    SUBMITTED: "SUBMITTED",
    UNDER_REVIEW: "UNDER_REVIEW",
    APPROVED: "APPROVED",
    REJECTED: "REJECTED",
    RESOLVED: "RESOLVED",
    CANCELLED: "CANCELLED",
});

const TERMINAL_CLAIM_STATUSES = Object.freeze([
    CLAIM_STATUS.REJECTED,
    CLAIM_STATUS.RESOLVED,
    CLAIM_STATUS.CANCELLED,
]);

const CLAIM_TRANSITIONS = Object.freeze({
    DRAFT: ["SUBMITTED", "CANCELLED"],
    SUBMITTED: ["UNDER_REVIEW", "CANCELLED"],
    UNDER_REVIEW: ["APPROVED", "REJECTED", "CANCELLED"],
    APPROVED: ["RESOLVED", "CANCELLED"],
    REJECTED: [],
    RESOLVED: [],
    CANCELLED: [],
});

module.exports = { CLAIM_STATUS, CLAIM_TRANSITIONS, TERMINAL_CLAIM_STATUSES };
