const CLAIM_STATUS = Object.freeze({
    SUBMITTED: "SUBMITTED",
    IN_PROGRESS: "IN_PROGRESS",
    RESOLVED: "RESOLVED",
    REJECTED: "REJECTED",
    CANCELLED: "CANCELLED",
});

const TERMINAL_CLAIM_STATUSES = Object.freeze([
    CLAIM_STATUS.RESOLVED,
    CLAIM_STATUS.REJECTED,
    CLAIM_STATUS.CANCELLED,
]);

module.exports = { CLAIM_STATUS, TERMINAL_CLAIM_STATUSES };
