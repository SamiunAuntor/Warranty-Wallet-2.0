import { useCallback, useEffect, useState } from "react";
import { router, useLocalSearchParams } from "expo-router";
import { ActivityIndicator, Alert, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from "react-native";
import { deleteAsset, getAsset, getCategories, updateAsset, type Asset, type Category } from "../../../lib/assets-api";
import { colors, spacing } from "../../../lib/theme";
import { useAuth } from "../../../providers/auth-provider";

export default function AssetDetailsScreen() { const { id } = useLocalSearchParams<{ id: string }>(); const { user } = useAuth(); const [asset, setAsset] = useState<Asset | null>(null); const [categories, setCategories] = useState<Category[]>([]); const [editing, setEditing] = useState(false); const [saving, setSaving] = useState(false); const [error, setError] = useState(""); const load = useCallback(async () => { if (!user || !id) return; try { const token = await user.getIdToken(); const [next, catalog] = await Promise.all([getAsset(token, id), getCategories()]); setAsset(next); setCategories(catalog); } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not load asset."); } }, [id, user]); useEffect(() => { void load(); }, [load]); async function save(input: Partial<Parameters<typeof updateAsset>[2]>) { if (!user || !asset) return; setSaving(true); try { setAsset(await updateAsset(await user.getIdToken(), asset.id, input)); setEditing(false); } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not update asset."); } finally { setSaving(false); } } function remove() { if (!user || !asset) return; Alert.alert("Delete asset?", `Remove ${asset.name} and its records?`, [{ text: "Cancel", style: "cancel" }, { text: "Delete", style: "destructive", onPress: async () => { try { await deleteAsset(await user.getIdToken(), asset.id); router.back(); } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not delete asset."); } } }]); } if (!asset) return <View style={styles.center}>{error ? <Text style={styles.error}>{error}</Text> : <ActivityIndicator color={colors.brand} />}</View>; return <ScrollView contentContainerStyle={styles.container}><Pressable onPress={() => router.back()}><Text style={styles.back}>Back to assets</Text></Pressable><Text style={styles.eyebrow}>{asset.category.name}</Text><Text style={styles.title}>{asset.name}</Text><Text style={styles.subtitle}>{asset.brand}{asset.model ? ` · ${asset.model}` : ""}</Text><View style={styles.card}><Info label="Warranty status" value={asset.warrantyStatus.replaceAll("_", " ")} /><Info label="Purchase date" value={new Date(asset.purchaseDate).toLocaleDateString()} /><Info label="Purchase price" value={asset.purchasePrice} /><Info label="Expiry date" value={asset.expiryDate ? new Date(asset.expiryDate).toLocaleDateString() : "No warranty expiry"} /><Info label="Serial number" value={asset.serialNumber ?? "Not provided"} /><Info label="Seller" value={asset.sellerName ?? "Not provided"} /></View>{editing ? <EditForm asset={asset} categories={categories} saving={saving} onSave={(input) => void save(input)} /> : <View style={styles.actions}><Pressable onPress={() => setEditing(true)} style={styles.primary}><Text style={styles.primaryText}>Edit asset</Text></Pressable><Pressable onPress={() => void save({ lifecycleStatus: "ARCHIVED" })} style={styles.outline}><Text style={styles.outlineText}>Archive</Text></Pressable><Pressable onPress={remove} style={styles.deleteButton}><Text style={styles.deleteText}>Delete</Text></Pressable></View>}{asset.documents?.length ? <View style={styles.card}><Text style={styles.section}>Documents</Text>{asset.documents.map((document) => <Text key={document.id} style={styles.document}>{document.fileName}</Text>)}</View> : null}</ScrollView>; }
function Info({ label, value }: { label: string; value: string }) { return <View style={styles.info}><Text style={styles.label}>{label}</Text><Text style={styles.value}>{value}</Text></View>; }
function EditForm({ asset, categories, saving, onSave }: { asset: Asset; categories: Category[]; saving: boolean; onSave: (input: Partial<Parameters<typeof updateAsset>[2]>) => void }) { const [name, setName] = useState(asset.name); const [brand, setBrand] = useState(asset.brand); const [model, setModel] = useState(asset.model ?? ""); const [serialNumber, setSerialNumber] = useState(asset.serialNumber ?? ""); const [purchasePrice, setPurchasePrice] = useState(asset.purchasePrice); const [purchaseDate, setPurchaseDate] = useState(asset.purchaseDate.slice(0, 10)); const [categoryId, setCategoryId] = useState(asset.category.id); return <View style={styles.card}><Text style={styles.section}>Edit details</Text><Field value={name} onChangeText={setName} placeholder="Name" /><Field value={brand} onChangeText={setBrand} placeholder="Brand" /><Field value={model} onChangeText={setModel} placeholder="Model" /><Field value={serialNumber} onChangeText={setSerialNumber} placeholder="Serial number" /><Field keyboardType="decimal-pad" value={purchasePrice} onChangeText={setPurchasePrice} placeholder="Purchase price" /><Field value={purchaseDate} onChangeText={setPurchaseDate} placeholder="Purchase date" /><Text style={styles.label}>Category</Text><View style={styles.categoryRow}>{categories.filter((item) => item.isActive).map((category) => <Pressable key={category.id} onPress={() => setCategoryId(category.id)} style={[styles.category, category.id === categoryId && styles.categorySelected]}><Text style={category.id === categoryId ? styles.categoryTextSelected : styles.categoryText}>{category.name}</Text></Pressable>)}</View><Pressable disabled={saving} onPress={() => onSave({ name: name.trim(), brand: brand.trim(), model: model.trim(), serialNumber: serialNumber.trim(), purchasePrice: Number(purchasePrice), purchaseDate, categoryId })} style={styles.primary}><Text style={styles.primaryText}>{saving ? "Saving..." : "Save changes"}</Text></Pressable></View>; }
function Field(props: { value: string; onChangeText: (value: string) => void; placeholder: string; keyboardType?: "default" | "decimal-pad" }) { return <TextInput {...props} placeholderTextColor={colors.muted} style={styles.input} />; }
const styles = StyleSheet.create({ container: { backgroundColor: colors.canvas, gap: spacing.md, padding: spacing.lg }, center: { alignItems: "center", flex: 1, justifyContent: "center", padding: spacing.lg }, back: { color: colors.brand, fontWeight: "700" }, eyebrow: { color: colors.brand, fontSize: 12, fontWeight: "700", letterSpacing: 2 }, title: { color: colors.ink, fontSize: 30, fontWeight: "800" }, subtitle: { color: colors.muted, fontSize: 15 }, card: { backgroundColor: colors.surface, borderColor: colors.border, borderRadius: 14, borderWidth: 1, padding: spacing.lg }, info: { borderBottomColor: colors.border, borderBottomWidth: 1, paddingVertical: spacing.sm }, label: { color: colors.muted, fontSize: 12 }, value: { color: colors.ink, fontSize: 15, fontWeight: "600", marginTop: spacing.xs }, actions: { gap: spacing.sm }, primary: { alignItems: "center", backgroundColor: colors.brand, borderRadius: 10, marginTop: spacing.md, padding: spacing.md }, primaryText: { color: colors.surface, fontWeight: "700" }, outline: { alignItems: "center", borderColor: colors.brand, borderRadius: 10, borderWidth: 1, padding: spacing.md }, outlineText: { color: colors.brand, fontWeight: "700" }, deleteButton: { alignItems: "center", borderColor: colors.danger, borderRadius: 10, borderWidth: 1, padding: spacing.md }, deleteText: { color: colors.danger, fontWeight: "700" }, delete: { color: colors.brand, fontSize: 12, fontWeight: "700" }, error: { color: colors.danger, textAlign: "center" }, section: { color: colors.ink, fontSize: 17, fontWeight: "700" }, document: { borderBottomColor: colors.border, borderBottomWidth: 1, color: colors.muted, paddingVertical: spacing.sm }, input: { borderColor: colors.border, borderRadius: 10, borderWidth: 1, color: colors.ink, marginTop: spacing.sm, padding: spacing.sm }, categoryRow: { flexDirection: "row", flexWrap: "wrap", gap: spacing.xs, marginTop: spacing.sm }, category: { borderColor: colors.border, borderRadius: 14, borderWidth: 1, paddingHorizontal: spacing.sm, paddingVertical: spacing.xs }, categorySelected: { backgroundColor: colors.brand, borderColor: colors.brand }, categoryText: { color: colors.muted, fontSize: 12 }, categoryTextSelected: { color: colors.surface, fontSize: 12 } });
