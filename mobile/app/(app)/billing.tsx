import * as WebBrowser from "expo-web-browser";
import { useCallback, useEffect, useState } from "react";
import { ActivityIndicator, Pressable, ScrollView, StyleSheet, Text, View } from "react-native";
import { cancelSubscription, confirmCheckout, createCheckout, getPayments, getSubscription, resumeSubscription, type Payment, type Plan, type Subscription } from "../../lib/billing-api";
import { colors, spacing } from "../../lib/theme";
import { useAuth } from "../../providers/auth-provider";

const plans: Array<{ name: Plan; description: string; price: string }> = [{ name: "BASIC", description: "Track your essential warranties.", price: "Free" }, { name: "PLUS", description: "More assets and richer reminders.", price: "Paid" }, { name: "PRO", description: "Full warranty management capacity.", price: "Paid" }];
export default function BillingScreen() { const { user } = useAuth(); const [subscription, setSubscription] = useState<Subscription>(null); const [payments, setPayments] = useState<Payment[]>([]); const [loading, setLoading] = useState(true); const [busy, setBusy] = useState(false); const [error, setError] = useState(""); const load = useCallback(async () => { if (!user) return; setLoading(true); try { const token = await user.getIdToken(); const [current, history] = await Promise.all([getSubscription(token), getPayments(token)]); setSubscription(current); setPayments(history.data); } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not load billing."); } finally { setLoading(false); } }, [user]); useEffect(() => { void load(); }, [load]); async function upgrade(plan: "PLUS" | "PRO") { if (!user) return; setBusy(true); setError(""); try { const token = await user.getIdToken(); const { url } = await createCheckout(token, plan); const result = await WebBrowser.openBrowserAsync(url); const returnedUrl = "url" in result ? result.url : ""; const sessionId = returnedUrl.match(/[?&]session_id=([^&]+)/)?.[1]; if (sessionId) await confirmCheckout(token, decodeURIComponent(sessionId)); await load(); } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not complete checkout."); } finally { setBusy(false); } } async function toggleCancel() { if (!user || !subscription) return; setBusy(true); try { const token = await user.getIdToken(); setSubscription(await (subscription.cancelAtPeriodEnd ? resumeSubscription(token) : cancelSubscription(token))); } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not update subscription."); } finally { setBusy(false); } } if (loading) return <View style={styles.center}><ActivityIndicator color={colors.brand} /></View>; return <ScrollView contentContainerStyle={styles.container}><Text style={styles.eyebrow}>PLAN AND PAYMENTS</Text><Text style={styles.title}>Billing</Text>{error ? <Text style={styles.error}>{error}</Text> : null}<View style={styles.card}><Text style={styles.section}>Current plan</Text><Text style={styles.current}>{subscription?.plan ?? "BASIC"}</Text><Text style={styles.muted}>{subscription?.isActive ? `Active until ${subscription.currentPeriodEnd ? new Date(subscription.currentPeriodEnd).toLocaleDateString() : "the current period"}.` : "You are using the basic plan."}</Text>{subscription && subscription.plan !== "BASIC" ? <Pressable disabled={busy} onPress={() => void toggleCancel()} style={styles.outline}><Text style={styles.outlineText}>{subscription.cancelAtPeriodEnd ? "Resume subscription" : "Cancel at period end"}</Text></Pressable> : null}</View><Text style={styles.section}>Available plans</Text>{plans.map((plan) => <View key={plan.name} style={styles.plan}><View style={styles.planCopy}><Text style={styles.planName}>{plan.name}</Text><Text style={styles.muted}>{plan.description}</Text></View>{plan.name === "BASIC" || subscription?.plan === plan.name ? <Text style={styles.activeText}>{subscription?.plan === plan.name ? "Current" : "Included"}</Text> : <Pressable disabled={busy} onPress={() => void upgrade(plan.name as "PLUS" | "PRO")} style={styles.upgrade}><Text style={styles.upgradeText}>Choose</Text></Pressable>}</View>)}<Text style={styles.section}>Payment history</Text>{payments.length ? payments.map((payment) => <View key={payment.id} style={styles.payment}><View><Text style={styles.planName}>{payment.plan ?? "Plan"}</Text><Text style={styles.muted}>{new Date(payment.createdAt).toLocaleDateString()}</Text></View><Text style={styles.paymentStatus}>{payment.status}</Text></View>) : <Text style={styles.muted}>No payments yet.</Text>}</ScrollView>; }
const styles = StyleSheet.create({ container: { backgroundColor: colors.canvas, gap: spacing.md, padding: spacing.lg }, center: { alignItems: "center", flex: 1, justifyContent: "center" }, eyebrow: { color: colors.brand, fontSize: 12, fontWeight: "700", letterSpacing: 2 }, title: { color: colors.ink, fontSize: 30, fontWeight: "800" }, section: { color: colors.ink, fontSize: 17, fontWeight: "700", marginTop: spacing.sm }, card: { backgroundColor: colors.surface, borderColor: colors.border, borderRadius: 14, borderWidth: 1, padding: spacing.lg }, current: { color: colors.brand, fontSize: 32, fontWeight: "800", marginTop: spacing.sm }, muted: { color: colors.muted, fontSize: 13, lineHeight: 20, marginTop: spacing.xs }, outline: { alignSelf: "flex-start", borderColor: colors.brand, borderRadius: 10, borderWidth: 1, marginTop: spacing.md, padding: spacing.sm }, outlineText: { color: colors.brand, fontSize: 12, fontWeight: "700" }, plan: { alignItems: "center", backgroundColor: colors.surface, borderColor: colors.border, borderRadius: 14, borderWidth: 1, flexDirection: "row", padding: spacing.md }, planCopy: { flex: 1 }, planName: { color: colors.ink, fontSize: 15, fontWeight: "700" }, upgrade: { backgroundColor: colors.brand, borderRadius: 9, paddingHorizontal: spacing.md, paddingVertical: spacing.sm }, upgradeText: { color: colors.surface, fontSize: 12, fontWeight: "700" }, activeText: { color: colors.brand, fontSize: 12, fontWeight: "700" }, payment: { alignItems: "center", backgroundColor: colors.surface, borderColor: colors.border, borderRadius: 12, borderWidth: 1, flexDirection: "row", justifyContent: "space-between", padding: spacing.md }, paymentStatus: { color: colors.brand, fontSize: 11, fontWeight: "700" }, error: { color: colors.danger } });
