import { useCallback, useEffect, useState } from "react";
import { ActivityIndicator, FlatList, Pressable, StyleSheet, Text, View } from "react-native";
import { getAdminAssets, getAdminClaims, getAdminPayments, updateAdminClaim, type AdminAsset, type AdminClaim, type AdminPayment } from "../../lib/admin-api";
import { colors, spacing } from "../../lib/theme";
import { useAuth } from "../../providers/auth-provider";
const claimStatuses = ["SUBMITTED", "IN_PROGRESS", "RESOLVED", "REJECTED", "CANCELLED"];
export default function AdminOperationsScreen() { const { user, appUser } = useAuth(); const [assets, setAssets] = useState<AdminAsset[]>([]); const [claims, setClaims] = useState<AdminClaim[]>([]); const [payments, setPayments] = useState<AdminPayment[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const load = useCallback(async () => { if (!user || appUser?.role !== "ADMIN") return; try { const token = await user.getIdToken(); const [nextAssets, nextClaims, nextPayments] = await Promise.all([getAdminAssets(token), getAdminClaims(token), getAdminPayments(token)]); setAssets(nextAssets); setClaims(nextClaims); setPayments(nextPayments); } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not load admin operations."); } finally { setLoading(false); } }, [appUser?.role, user]); useEffect(() => { void load(); }, [load]); async function change(claim: AdminClaim, status: string) { if (!user) return; try { const updated = await updateAdminClaim(await user.getIdToken(), claim.id, status); setClaims((current) => current.map((item) => item.id === updated.id ? updated : item)); } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not update claim."); } } if (appUser?.role !== "ADMIN") return <View style={styles.center}><Text style={styles.title}>Admin access required.</Text></View>; if (loading) return <View style={styles.center}><ActivityIndicator color={colors.brand} /></View>; return <FlatList contentContainerStyle={styles.container} data={[{ title: "Customer assets", count: assets.length, body: assets.map((item) => `${item.name} · ${item.user.email}`) }, { title: "Payments", count: payments.length, body: payments.map((item) => `${item.plan ?? "Plan"} · ${item.amount} ${item.currency} · ${item.status}`) }]} keyExtractor={(item) => item.title} ListHeaderComponent={<><Text style={styles.eyebrow}>ADMIN OPERATIONS</Text><Text style={styles.title}>Assets, claims, payments</Text>{error ? <Text style={styles.error}>{error}</Text> : null}<View style={styles.card}><Text style={styles.section}>Claims</Text>{claims.map((claim) => <View key={claim.id} style={styles.claim}><Text style={styles.name}>{claim.claimNumber} · {claim.title}</Text><Text style={styles.muted}>{claim.product.name} · {claim.user.email}</Text><View style={styles.statusRow}>{claimStatuses.map((status) => <Pressable key={status} onPress={() => void change(claim, status)} style={[styles.status, claim.status === status && styles.selected]}><Text style={claim.status === status ? styles.selectedText : styles.statusText}>{status.replaceAll("_", " ")}</Text></Pressable>)}</View></View>)}</View></>} renderItem={({ item }) => <View style={styles.card}><Text style={styles.section}>{item.title} ({item.count})</Text>{item.body.slice(0, 50).map((line, index) => <Text key={`${line}-${index}`} style={styles.muted}>{line}</Text>)}</View>} />; }
const styles = StyleSheet.create({ container: { backgroundColor: colors.canvas, gap: spacing.md, padding: spacing.lg }, center: { alignItems: "center", flex: 1, justifyContent: "center" }, eyebrow: { color: colors.brand, fontSize: 12, fontWeight: "700", letterSpacing: 2 }, title: { color: colors.ink, fontSize: 28, fontWeight: "800" }, error: { color: colors.danger }, card: { backgroundColor: colors.surface, borderColor: colors.border, borderRadius: 14, borderWidth: 1, padding: spacing.md }, section: { color: colors.ink, fontSize: 17, fontWeight: "700" }, claim: { borderTopColor: colors.border, borderTopWidth: 1, marginTop: spacing.sm, paddingTop: spacing.sm }, name: { color: colors.ink, fontSize: 14, fontWeight: "700" }, muted: { color: colors.muted, fontSize: 12, lineHeight: 19, marginTop: spacing.xs }, statusRow: { flexDirection: "row", flexWrap: "wrap", gap: spacing.xs, marginTop: spacing.sm }, status: { borderColor: colors.border, borderRadius: 12, borderWidth: 1, paddingHorizontal: spacing.xs, paddingVertical: 4 }, selected: { backgroundColor: colors.brand, borderColor: colors.brand }, statusText: { color: colors.muted, fontSize: 9 }, selectedText: { color: colors.surface, fontSize: 9, fontWeight: "700" } });
