"use client";
import { useEffect, useState, type FormEvent } from "react";
import { FolderPlus, Pencil, Power, PowerOff, Search } from "lucide-react";
import { ActionIconButton } from "@/components/ui/action-icon-button";
import { Loading } from "@/components/ui/loading";
import { AdminBadge, AdminEmpty, AdminModal, AdminPageHeader, AdminPagination, AdminTable, AdminTableHead, adminCard, adminInput } from "@/components/admin/admin-ui";
import { useAuth } from "@/contexts/auth-context";
import { createCategory, deleteCategory, getAdminCategories, updateCategory, type Category } from "@/lib/admin-api";
import { dialog, toast } from "@/lib/notifications";
import { readQuery, replaceQuery } from "@/lib/url-query";
export default function CategoriesPage() {
  const { firebaseUser } = useAuth(); const [items, setItems] = useState<Category[]>([]); const [editing, setEditing] = useState<Category | null>(null); const [open, setOpen] = useState(false); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [reload, setReload] = useState(0);
  const [search, setSearch] = useState(() => readQuery("search")); const [status, setStatus] = useState(() => readQuery("status")); const [sort, setSort] = useState(() => readQuery("sort", "name:asc")); const [page, setPage] = useState(() => Number(readQuery("page", "1")) || 1); const [meta, setMeta] = useState<{ totalPages: number } | null>(null);
  useEffect(() => { if (!firebaseUser) return; const [sortBy, sortOrder] = sort.split(":"); setLoading(true); firebaseUser.getIdToken().then((token) => getAdminCategories(token, { page, limit: 15, search, status, sortBy, sortOrder })).then((result) => { setItems(result.data); setMeta(result.meta); }).catch((error) => toast.error(error.message)).finally(() => setLoading(false)); }, [firebaseUser, page, reload, search, sort, status]);
  useEffect(() => replaceQuery({ search, status, sort, page }), [page, search, sort, status]);
  const showForm = (item: Category | null = null) => { setEditing(item); setOpen(true); };
  const save = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!firebaseUser) return; const form = new FormData(event.currentTarget); const input = { name: String(form.get("name")).trim(), description: String(form.get("description")).trim() }; setSaving(true); try { const token = await firebaseUser.getIdToken(); if (editing) await updateCategory(token, editing.id, input); else await createCategory(token, input); toast.success(editing ? "Category updated." : "Category added."); setOpen(false); setReload((v) => v + 1); } catch (error) { toast.error(error instanceof Error ? error.message : "Category could not be saved."); } finally { setSaving(false); } };
  const toggle = async (item: Category) => {
    if (!firebaseUser) return;
    const activating = !item.isActive;
    if (!activating && !(await dialog.confirm("Deactivate category?", `${item.name} will no longer be available for new assets.`)).isConfirmed) return;
    try {
      const token = await firebaseUser.getIdToken();
      if (activating) await updateCategory(token, item.id, { isActive: true });
      else await deleteCategory(token, item.id);
      toast.success(activating ? "Category activated." : "Category deactivated.");
      setReload((value) => value + 1);
    } catch (error) {
      toast.error(error instanceof Error ? error.message : "Category status could not be updated.");
    }
  };
  return <div className="mx-auto max-w-[1200px] pb-10"><AdminPageHeader title="Categories" description="Organize the catalog options customers use for their assets." action={<button onClick={() => showForm()} className="inline-flex h-11 items-center gap-2 rounded-xl bg-[#5b47ee] px-4 text-sm font-semibold text-white"><FolderPlus className="h-4 w-4"/>Add category</button>}/><section className={`${adminCard} mt-6 grid gap-3 p-4 md:grid-cols-[1fr_auto_auto]`}><label className="relative"><Search className="absolute left-3 top-3 h-5 w-5 text-[#858b98]"/><input value={search} onChange={(e) => { setSearch(e.target.value); setPage(1); }} placeholder="Search categories" className={`${adminInput} w-full pl-10`}/></label><select value={status} onChange={(e) => { setStatus(e.target.value); setPage(1); }} className={adminInput}><option value="">All statuses</option><option value="active">Active</option><option value="inactive">Inactive</option></select><select value={sort} onChange={(e) => { setSort(e.target.value); setPage(1); }} className={adminInput}><option value="name:asc">Name A–Z</option><option value="name:desc">Name Z–A</option><option value="createdAt:desc">Newest first</option><option value="createdAt:asc">Oldest first</option></select></section>{loading ? <Loading fullScreen={false}/> : items.length === 0 ? <AdminEmpty title="No categories found" description="Change the filters or add a category."/> : <AdminTable><AdminTableHead><th className="px-5 py-4">Category</th><th className="px-4 py-4">Assets</th><th className="px-4 py-4">Status</th><th className="px-5 py-4 text-right">Actions</th></AdminTableHead><tbody className="divide-y divide-[#edf0f5]">{items.map((item) => <tr key={item.id}><td className="px-5 py-4"><p className="font-semibold">{item.name}</p><p className="text-xs text-[#747b89]">{item.description || "No description"}</p></td><td className="px-4 py-4">{item._count?.products ?? 0}</td><td className="px-4 py-4"><AdminBadge tone={item.isActive ? "success" : "neutral"}>{item.isActive ? "ACTIVE" : "INACTIVE"}</AdminBadge></td><td className="px-5 py-4"><div className="flex justify-end"><ActionIconButton icon={Pencil} label="Edit category" tone="primary" onClick={() => showForm(item)}/><ActionIconButton icon={item.isActive ? PowerOff : Power} label={item.isActive ? "Deactivate category" : "Activate category"} tone={item.isActive ? "danger" : "primary"} onClick={() => void toggle(item)}/></div></td></tr>)}</tbody></AdminTable>}<AdminPagination page={page} totalPages={meta?.totalPages ?? 1} onChange={setPage}/><AdminModal open={open} onClose={() => setOpen(false)} title={editing ? "Edit category" : "Add category"} size="sm"><form onSubmit={save} className="space-y-4"><input name="name" required defaultValue={editing?.name} placeholder="Category name" className={`${adminInput} w-full`}/><textarea name="description" defaultValue={editing?.description ?? ""} placeholder="Description" rows={4} className="w-full rounded-xl border border-[#dfe3ed] p-3"/><div className="flex justify-end gap-2"><button type="button" onClick={() => setOpen(false)} className="px-4">Cancel</button><button disabled={saving} className="rounded-xl bg-[#5b47ee] px-5 py-3 text-sm font-semibold text-white">{saving ? "Saving..." : "Save"}</button></div></form></AdminModal></div>;
}
